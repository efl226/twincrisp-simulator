// TwinCrisp panel face — starts from scratch. Replace this placeholder
// with the real layout once Figma refs / SVG assets are in.
export default function Panel({ S, C, send }) {
  return (
    <div className="panelwrap">
      <div className="panel panel-placeholder">
        <span className="placeholder-label">TWINCRISP PANEL — TODO</span>
      </div>
    </div>
  )
}
