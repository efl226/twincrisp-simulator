Drop the Neue Haas Grotesk Display Pro .ttf files here:

- `NeueHaasDisplayRoman.ttf` (weight 400)
- `NeueHaasDisplayMedium.ttf` (weight 500)

Filenames are wired up in `src/styles.css` — rename on either side if yours differ.
